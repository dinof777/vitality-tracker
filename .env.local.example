# Copy this file to .env.local and fill in your Neon connection string.
# Neon Console → your project → Connection string (URI). Server-only secret —
# do NOT prefix with NEXT_PUBLIC; all DB access happens in API routes.
# .env.local is gitignored.

DATABASE_URL=postgresql://USER:PASSWORD@HOST/vitality_tracker?sslmode=require
